## 1. Fix the build context

- [x] 1.1 Add a `.dockerignore` excluding `node_modules`, `.next`, `.git`, `.env*` and local tooling output.
- [x] 1.2 Reorder the `Dockerfile` builder stage: copy `package.json` and `package-lock.json` first, run `npm ci`, then copy the source — removing the duplicated `COPY`.
- [x] 1.3 Confirm the runner stage still carries `prisma/schema.prisma` and that `postinstall` can run `prisma generate`.

## 2. Prepare the GCP project

- [x] 2.1 Enable the Cloud Run, Cloud Build, Artifact Registry and Secret Manager APIs on `iron-haiku-507914-i0`.
- [x] 2.2 Create an Artifact Registry Docker repository in `asia-southeast1`.
- [x] 2.3 Create Secret Manager secrets for `DATABASE_URL`, `DIRECT_URL` and `JWT_SECRET` from the local `.env` values, without modifying `.env`.
- [x] 2.4 Grant the Cloud Build and Cloud Run service accounts access to those secrets, and give Cloud Build permission to deploy to Cloud Run.

## 3. Build configuration

- [x] 3.1 Add `cloudbuild.yaml` that builds the image with all six values as build args, reading the three secrets via `availableSecrets` and taking `NEXT_PUBLIC_BASE_URL` from a substitution.
- [x] 3.2 Tag each image with both `$SHORT_SHA` and `latest`, and push both.
- [x] 3.3 Add a deploy step that rolls the new image out to Cloud Run with the secrets bound via `--set-secrets`.

## 4. First deployment

- [x] 4.1 Deploy the service once with a placeholder base URL to obtain its real URL.
- [x] 4.2 Read the assigned URL back from the service.
- [x] 4.3 Rebuild and redeploy with `NEXT_PUBLIC_BASE_URL` set to that URL, so the client bundle carries the correct value.
- [x] 4.4 Confirm the service responds and that a share link generated by the app points at the new URL.

## 5. Continuous deployment

- [x] 5.1 Attempt the Cloud Build GitHub connection; confirm it requires browser OAuth and repository admin, and remove it.
- [x] 5.2 Store the three `NEXT_PUBLIC_*` values in Secret Manager too, and rewrite `cloudbuild.yaml` to read every value from there so no configuration has to be passed in.
- [x] 5.3 Create a `github-deployer` service account with the roles needed to run a build and deploy to Cloud Run.
- [x] 5.4 Create a Workload Identity Federation pool and OIDC provider pinned to `subzeroxppil/events`, and let that identity impersonate the deployer.
- [x] 5.5 Add `.github/workflows/deploy.yml` that authenticates by WIF and submits the build on pushes to `master`.
- [x] 5.6 Verify the whole pipeline by running `cloudbuild.yaml` with every value sourced from Secret Manager.

## 6. Document

- [x] 6.1 Update `README.md` with the new service URL, the project it lives in, and how the deployment is configured.
- [x] 6.2 Note the outstanding GitHub App authorization step and what it unblocks.
